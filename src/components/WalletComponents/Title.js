import styled from "styled-components";

const Title = styled.div`
    font-weight: bold;
    font-size: 26px;
    line-height: 31px;

    margin-bottom: 22px;

    color: #FFFFFF;
`;

export default Title;